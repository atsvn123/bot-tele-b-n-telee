import { Client, StorageAdapter } from './deps.node.js';
export declare class RedisAdapter<T> implements StorageAdapter<T> {
    private redis;
    private readonly ttl?;
    /**
     * @constructor
     * @param {opts} Constructor options
     * @param {opts.ttl} ttl - Session time to life in SECONDS.
     * @param {opts.instance} instance - Instance of redis.
     */
    constructor({ instance, ttl }: {
        instance?: Client;
        ttl?: number;
    });
    read(key: string): Promise<T>;
    write(key: string, value: T): Promise<void>;
    delete(key: string): Promise<void>;
}
